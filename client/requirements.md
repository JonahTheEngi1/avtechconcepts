## Packages
framer-motion | Page transitions and scroll-triggered animations
react-intersection-observer | Triggering animations when elements enter viewport
lucide-react | Icons

## Notes
The contact form expects POST /api/contact to return { success: true, message: string }
No additional integration assumptions required.
